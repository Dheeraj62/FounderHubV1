export const environment = {
    production: true,
    /**
     * Same-origin `/api/...` works when your host (e.g. Vercel) rewrites `/api/*` to your API.
     * If you do not use a proxy, set the full base URL instead, e.g.:
     * `apiUrl: 'https://founderhubv1.onrender.com/api/v1'`
     */
    apiUrl: '/api/v1'
};
