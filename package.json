{
  "name": "founderhub",
  "private": true,
  "scripts": {
    "dev": "npm start --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "backend:dev": "dotnet run --project backend/FounderHub.Api",
    "build": "npm run frontend:build",
    "lint": "npm run lint --prefix frontend"
  }
}